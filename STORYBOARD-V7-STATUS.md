# 千幕分镜 V7：生产现状与设计基线

> 对应扩展版本：1.55.1  
> 数据契约：Storyboard Schema 7  
> 本文是当前分镜开发的唯一现状说明。其他 `STORYBOARD-*` 文档只保留为历史方案，不代表生产入口已采用其中的原型架构。

## 1. 当前生产运行链

生产入口只使用以下链路：

1. `index.js`：SillyTavern 生命周期、正文事件、界面、自动取景、队列、生成、挂载与重绘。
2. `qianmu-storyboard.js`：分镜数据契约、迁移、模型能力、镜组模板、路由与安全清理。
3. `server-plugin.js` 与 `qianmu-image-gateway.js`：同源生成网关。
4. `style.css`：生产界面样式。

`qianmu-storyboard-frame.js`、`qianmu-storyboard-integration.js`、`qianmu-storyboard-*-ui.js`、`qianmu-storyboard-*-modal.js`、`qianmu-storyboard-storage.js` 等文件属于早期并行原型。它们目前不从 `index.js` 导入、不注册生命周期、不注入样式，也不持有生产状态。保留这些文件是为了稳定期内核对能力，不代表它们仍是有效运行代码；后续只有在功能对齐并完成迁移测试后才可删除。

这条边界由 `tests/storyboard-single-runtime.test.mjs` 固化，避免再次出现重复导出、重复初始化或原型 API 迁移不完整导致扩展无法加载。

## 2. 设计意图

分镜不是正文前的一道工作流，而是正文后的安静能力：

- 默认无感：新回复出现后，千幕自行判断是否值得配图；值得才规划并生成，不值得则静默跳过。
- 正文优先：取景、排队、跳过和失败的中间态不占据正文；成片才出现。
- 修改后置：成片保留修改、重绘、预览、下载和移出正文；高级编辑进入镜头台详情页。
- 新手零决策：默认开启提示词整理和无感自动配图，镜组采用智能模板。
- 高手可控：可选择镜组模板、镜头上限、并发、模型路由、参数样式、人物档案和 ComfyUI 工作流。
- 稳定优先：非分镜模块不改动，生成仍走既有同源网关，不改写 SillyTavern 全局生图设置。

## 3. 默认用户流程

```text
新回复
  → 判断是否具有新增画面价值
    → 否：静默结束
    → 是：按当前模板规划 1–4 个互不重复的镜头
      → 选择模型并在需要时做后台内容适配
        → 按配置并发生成
          → 成片挂入正文
```

正文中只保留成片和克制的操作口。用户需要修改时，再打开详情页调整镜头标题、叙事职责、画面目的、提示词、人物、模型或参数并重绘。

未启用镜组路由时，行为保持为常规单图生成，继续使用生图页当前模型、参数、单次数量和正文挂载设置。

## 4. 镜组设计

镜组不是固定的“全景、近景、特写”三张套图，而是叙事职责不同的一组画面。当前内置：

- 智能镜组：按画面价值选择 1–4 镜。
- 三拍叙事：建立关系、推进动作、情绪落点；无价值的节拍可省略。
- 对话组：同框、越肩/单人近景、反应镜头。
- 动作组：环境与站位、关键动作、冲击或结果。
- 氛围组：环境、人物状态、细节物件共同建立情绪。

每个镜头保存 `title`、`role`、`purpose`、`shotType` 与提示词，详情页可独立修改和重绘。镜组并发限制为 1–4，并受总队列上限保护。

## 5. 内容适配

SFW/NSFW 不再作为前台镜头标签或路由条件。

- NAI Full 与 NAI V3：保留原叙事尺度。
- 受限制模型：仅在敏感镜头实际分配到该模型时，使用整理模型生成的安全、非露骨但叙事等价画面。
- ComfyUI：按自定义工作流处理，不替用户猜测能力。

该规则只在配置页固定说明，不在每个生成镜头下重复显示。适配结果仍记录在任务快照中，便于诊断，但不干扰沉浸体验。

## 6. 已落实的稳定性约束

- 生产入口只有一套分镜运行时。
- Schema 7 会清理旧 `sensitive` 路由条件并保留用户显式开关选择。
- 自动流程遇到无画面价值正文会静默跳过。
- 自动生成失败不在正文留下失败卡；诊断仍可在日志查看。
- 正文删除、编辑、切换 swipe 或切换聊天后，不把旧任务误挂到新正文。
- 进行中任务可放弃收片，等待任务可独立移除或清空。
- 模型能力按具体模型裁剪，密钥仍由 SillyTavern 密钥库管理。
- 版本 1.55.1 刷新入口、样式与图标模块缓存，避免继续加载 1.55.0。

## 7. 验收与后续边界

当前自动化测试共 47 项，全部通过，覆盖启动、模块连接、单运行时、自动流程、模型路由、参数、提示词整理、正文生命周期、日志、网关与非分镜回归。

稳定期后再处理的工作：

1. 在真实 SillyTavern 会话中完成多供应商、长对话、移动端与中断恢复的人工体验验收。
2. 收集镜组规划质量与误触发样本，微调“值得配图”的判断，不增加默认步骤。
3. 原型文件只有在确认不存在仍需迁移的能力后，单独提交删除；不要与功能迭代混在同一版本。

